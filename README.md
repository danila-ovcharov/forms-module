# forms-module
Simple forms module with 2 parameters:

1) formSelector - <form>
2) modalTimerId - const modalTimerId = setTimeout(() => openModal('.modal', modalTimerId), 50000);

Example of forms call():
<br>
forms('form', modalTimerId);
